apiVersion: v1 
kind: Pod
metadata:
  name: sdn
spec:
  nodeName: cloudran
  restartPolicy: Never
  containers:
  - name: sdn
    image: ocebenzer/udpsdn
    resources:
      limits:
        cpu: "500m"
        memory: "512M"
    command: ["/app/sdn"]
    stdin: true
    tty: true